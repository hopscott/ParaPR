#!/bin/bash
# Auto-activate Poetry virtual environment and load environment variables

# Use Poetry's virtual environment
layout poetry

# Load .env file if it exists (for environment variables)
dotenv_if_exists

# Optional: Show which Python is being used
echo "Python: $(which python)"
echo "Poetry venv: $(poetry env info --path 2>/dev/null || echo 'not yet created')"

